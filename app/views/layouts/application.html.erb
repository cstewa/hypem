<!DOCTYPE html>
<html>
<head>
  <title>Hypem</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div id= "navbar">
  <% if current_user %>
    <div id= "#nav_logout">
      <p><%=link_to 'Logout', logout_path %></p>
      <p> Logged in as: <%=current_user.hypem %></p>
    </div>
  <% else %>
    <div id= "#nav_signup">
      <p><%=link_to 'Sign Up', new_user_path, remote: true %></p>
    </div>
    <div id= "#nav_login">
      <p><%=link_to 'Login', login_path, remote: true %></p>
    </div>
  <% end %>
</div>

<div id= '#modal'>
  <div class= 'modal_wrap'>
    <div class= 'modal_overlay'>
    </div>
    <div class= 'vertical_offset'>
        <div class= 'modal_box'>
        </div>
    </div>
  </div>
</div>

<div id= "container">
<%= yield %>
</div>

</body>
</html>
