<!DOCTYPE html>
<html>
<head>
  <title>Hypem</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<% flash.each do |name, msg| -%>
  <%= content_tag :div, msg, class: name %>
<% end -%>

<div id= "navbar">
  <p><%=link_to 'Sign Up', new_user_path, remote: true %></p>
  <p><%=link_to 'Login', login_path, remote: true %></p>
  <p><%=link_to 'Authenticate with Soundcloud', @soundcloud_client.authorize_url() %></p>
</div>

<div id= '#modal'>
  <div class= 'modal_wrap'>
    <div class= 'modal_overlay'>
    </div>
    <div class= 'vertical_offset'>
        <div class= 'modal_box'>
        </div>
    </div>
  </div>
</div>

<div id= "container">
  <%= yield %>
</div>

</body>
</html>
